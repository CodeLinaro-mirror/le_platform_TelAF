maxThreads: 600
sandboxed: false

executables:
{
    tafMngdPMUnitTest =
            ($TELAF_ROOT/apps/tafMngdServices/tafMngdTest/tafMngdPMTest/tafMngdPMUnitTest/tafMngdPMUnitTestComp)
}

start: manual

processes:
{
    run:
    {
        (tafMngdPMUnitTest)
    }

    faultAction: stopApp

    #if ${LE_CONFIG_SERVICES_WATCHDOG} = y
        maxWatchdogTimeout: 100000
    #else
        watchdogTimeout: never
    #endif
}

bindings:
{
    tafMngdPMUnitTest.tafMngdPMUnitTestComp.taf_mngdPm -> tafMngdPMSvc.taf_mngdPm
    tafMngdPMUnitTest.tafMngdPMUnitTestComp.taf_pm -> tafPMSvc.taf_pm
}
