maxThreads: 600
sandboxed: false

username: telaf

capability:
{
        CAP_WAKE_ALARM
        CAP_NET_BIND_SERVICE
        CAP_NET_ADMIN
        CAP_BLOCK_SUSPEND

#if ${LE_CONFIG_TEST_COVERAGE} = y
        CAP_DAC_READ_SEARCH
        CAP_DAC_OVERRIDE
#endif
}

groups:
{
        system
        diag
        radio
        inet
        locclient
}

executables:
{
    tafThermSvc = ( $TELAF_ROOT/components/tafThermSvc )
}

start: auto

processes:
{
    run:
    {
        (tafThermSvc)
    }

    maxFileBytes: 8192K
    faultAction: restart
}

bindings:
{
}

extern:
{
    tafThermSvc.tafThermSvc.taf_therm
}