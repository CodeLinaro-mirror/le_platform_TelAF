# Copyright (c) 2024 Qualcomm Innovation Center, Inc. All rights reserved.
# SPDX-License-Identifier: BSD-3-Clause-Clear

policy_module(tafIvssSimSvc, 1.0)

gen_require(`
  type sysfs_t;
  type systemd_tmpfiles_t;
  type tmp_t;
  type proc_net_t;
  type unreserved_port_t;
  type node_t;
')

# Framework definition for telaf
type telaf_tafIvssSimSvc_exec_t;
files_type(telaf_tafIvssSimSvc_exec_t);

type telaf_tafIvssSimSvc_t;

type telaf_tafIvssSimSvc_data_t;
files_type(telaf_tafIvssSimSvc_data_t);

# Run the service in its own confined domain
init_telaf_app_domain(telaf_tafIvssSimSvc_t,telaf_tafIvssSimSvc_exec_t)

# Allow update daemon to install/uninstall this service
telaf_admin_manage_all_files_perms(telaf_tafIvssSimSvc_data_t)
telaf_admin_manage_all_files_perms(telaf_tafIvssSimSvc_exec_t)

# Allow systemd to get attribute of /tmp/vsomeip-0
allow systemd_tmpfiles_t tmp_t:sock_file { getattr };

# Allow tafIvssSimSvc to manipulate dir in tmpfs
allow telaf_tafIvssSimSvc_t tmp_t:dir manage_dir_perms;

# Allow tafIvssSimSvc to manipulate file in tmpfs
allow telaf_tafIvssSimSvc_t tmp_t:file manage_file_perms;

# Allow tafIvssSimSvc to manipulate sock file in tmpfs
allow telaf_tafIvssSimSvc_t tmp_t:sock_file manage_sock_file_perms;

# Allow tafIvssSimSvc to manipulate udp/tcp socket
allow telaf_tafIvssSimSvc_t self:udp_socket create_socket_perms;
allow telaf_tafIvssSimSvc_t self:tcp_socket create_stream_socket_perms;

# Allow tafIvssSimSvc to manipulate netlink_route_socket
allow telaf_tafIvssSimSvc_t self:netlink_route_socket rw_netlink_socket_perms;

# Allow telaf_tafIvssSimSvc_t to manipulate capabilites
allow telaf_tafIvssSimSvc_t self:capability { net_admin };
allow telaf_tafIvssSimSvc_t self:capability { sys_resource };

# Misc rules for network socket
allow telaf_tafIvssSimSvc_t proc_net_t:file read_file_perms;
allow telaf_tafIvssSimSvc_t { unreserved_port_t node_t }:udp_socket { name_bind node_bind };
allow telaf_tafIvssSimSvc_t { unreserved_port_t node_t }:tcp_socket { name_bind node_bind name_connect };

# Allow telaf_tafIvssSimSvc_t and telaf_tafSomeipGWSvc_t to connectto unix_stream_socket
telaf_tafSomeipGWSvc_connect(telaf_tafIvssSimSvc_t)