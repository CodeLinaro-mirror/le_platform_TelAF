sandboxed: false

start: auto

maxThreads: 600

username: telaf

capability:
{
        CAP_WAKE_ALARM
        CAP_NET_ADMIN
        CAP_BLOCK_SUSPEND
        CAP_DAC_READ_SEARCH
        CAP_DAC_OVERRIDE
}

executables:
{
    tafDiagSvc = ( $TELAF_ROOT/components/tafDiagSvc )
}

groups:
{
}

processes:
{
    run:
    {
        (tafDiagSvc)
    }

    faultAction: restart
}

bindings:
{
#if ${LE_CONFIG_DIAG_VSTACK} = y
    tafDiagSvc.tafDiagSvc.taf_diagDIDBackend -> tafDiagBackendSvc.taf_diagDIDBackend
    tafDiagSvc.tafDiagSvc.taf_diagSecBackend -> tafDiagBackendSvc.taf_diagSecBackend
#endif
}

extern:
{
    tafDiagSvc.tafDiagSvc.taf_diagDataID
    tafDiagSvc.tafDiagSvc.taf_diagSecurity
#if ${LE_CONFIG_DIAG_VSTACK} = ""
    tafDiagSvc.tafDiagSvc.taf_diagReset
    tafDiagSvc.tafDiagSvc.taf_diagRoutineCtrl
    tafDiagSvc.tafDiagSvc.taf_diagUpdate
    tafDiagSvc.tafDiagSvc.taf_diagEvent
#endif
}
