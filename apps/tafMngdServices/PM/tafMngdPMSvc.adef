maxThreads: 600

username: telaf
startGroup: 20

capability:
{
        CAP_WAKE_ALARM
        CAP_NET_ADMIN
}

groups:
{
        system
        diag
        radio
        inet
        locclient
        shutdown
}

executables:
{
        tafMngdPMSvc = ( $TELAF_ROOT/apps/tafMngdServices/PM/Components/tafMngdPMSvc )
}

start: manual

processes:
{
        run:
        {
                (tafMngdPMSvc)
        }

        maxCoreDumpFileBytes: 100000K
        maxFileBytes: 100000K
        faultAction: restart
        watchdogTimeout: 61000
        watchdogAction: restart
}

sandboxed: false

bindings:
{
    tafMngdPMSvc.tafMngdPMSvc.taf_pm -> tafPMSvc.taf_pm
    tafMngdPMSvc.tafMngdPMSvc.le_appInfo -> <tafcore>.le_appInfo
    tafMngdPMSvc.tafMngdPMSvc.taf_rpcPm -> <telaf>.rpcServer_102_telaf_taf_pm
    tafMngdPMSvc.watchdogChain.le_wdog -> <tafcore>.le_wdog
}

bundles:
{
    file:
    {
        [rw]    $TELAF_ROOT/apps/tafMngdServices/PM/Components/tafMngdPMSvc/config/tafMngdPMSvc.json   /
    }
}

// Directory in which configuration JSONs will be installed
requires:
{
    dir:
    {
        /data/ManagedServices /
    }
}

extern:
{
    tafMngdPMSvc.tafMngdPMSvc.taf_mngdPm
}
