sandboxed: false

start: auto

maxThreads: 600

username: telaf

capability:
{
        CAP_WAKE_ALARM
        CAP_NET_ADMIN
        CAP_BLOCK_SUSPEND

#if ${LE_CONFIG_TEST_COVERAGE} = y
        CAP_DAC_READ_SEARCH
        CAP_DAC_OVERRIDE
#endif
}

executables:
{
    tafDiagBackendSvc = ( $TELAF_ROOT/components/tafDiagBackendSvc )
}

groups:
{
}

processes:
{
    run:
    {
        (tafDiagBackendSvc)
    }
    maxFileBytes: 8192K
    faultAction: restart
}

bindings:
{
}

extern:
{
    tafDiagBackendSvc.tafDiagBackendSvc.taf_diagBackend
}