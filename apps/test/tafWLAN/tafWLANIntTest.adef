maxThreads: 600
sandboxed: true

executables:
{
    wlanTest = ( wlanIntTestComponent )
}

start: manual

processes:
{
    run:
    {
        (wlanTest)
    }

    faultAction: stopApp

    #if ${LE_CONFIG_SERVICES_WATCHDOG} = y
        maxWatchdogTimeout: 100000
    #else
        watchdogTimeout: never
    #endif
}

bindings:
{
    wlanTest.wlanIntTestComponent.taf_wlan -> tafWlanSvc.taf_wlan
}
