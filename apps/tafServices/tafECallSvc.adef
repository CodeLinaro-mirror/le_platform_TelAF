maxThreads: 300

capability:
{
        CAP_WAKE_ALARM
        CAP_NET_RAW
        CAP_NET_ADMIN
        CAP_BLOCK_SUSPEND
}

groups:
{
        system
        diag
        radio
        inet
        locclient
}

sandboxed: false

executables:
{
    tafECallSvc = ( $TELAF_ROOT/components/tafECallSvc )
}

start: auto

processes:
{
    run:
    {
        (tafECallSvc)
    }

    maxFileBytes: 8192K
    faultAction: restart
}

requires:
{
    configTree:
    {
        [w] .
    }
}

bindings:
{
    tafECallSvc.tafECallSvc.taf_sim -> tafSimCardSvc.taf_sim
    tafECallSvc.taf_pa_ecall.taf_sim -> tafSimCardSvc.taf_sim
}

extern:
{
    tafECallSvc.tafECallSvc.taf_ecall
}
